import 'package:flutter/material.dart';

class SocialNotifcationScreen extends StatelessWidget {
  const SocialNotifcationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Notifcation");
  }
}
